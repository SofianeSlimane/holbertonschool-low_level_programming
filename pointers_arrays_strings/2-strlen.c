#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
  int i;

  for (i = 0; i < *s ; i++)
    {
      if (i < *s)
	{
 
	  _putchar(s[0]);
	}
      
       
}
  return (i);
    }      

