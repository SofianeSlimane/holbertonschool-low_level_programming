#include "main.h"
#include <stdio.h>

/**
 * _strlen - check the code
 *@s: chaine de caracteres
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'  ; i++)
{
}
return (i);
}
