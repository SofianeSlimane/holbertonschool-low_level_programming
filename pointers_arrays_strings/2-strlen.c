#include "main.h"
#include <stdio.h>

/**
 * _strlen - check the code
 *@s: chaine de caracteres
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
int taille;
for (taille = 0; s[taille] != '\0'  ; taille++)
{
}
 for (i = taille -1; i >= 0; i--)
   {
     printf("%c", s[i]);
return (i);
}
