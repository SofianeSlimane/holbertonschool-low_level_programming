#include "main.h"


char *_strcpy(char *dest, char *src)
{
  int i;
  int j;
  for (i = 0; src[i] != '\0' || src[i] == '\0'; i++)
    {
    }
  
  dest = src;
  for (j = 0; j <= i; j++)
	 {
	   _putchar(dest[j]);
	 }
  return (dest);
}
