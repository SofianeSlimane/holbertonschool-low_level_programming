#include "main.h"


char *_strcpy(char *dest, char *src)
{
  int i;
  int j;
  for (i = 0; src[i] != '\0' || src[i] == '\0'; i++)
    {
    }
  i = 0;

  for (j = 0; dest[j] == i[src]; j++)
    {
      dest[j] += i[src];
    }
  return (0);
}
 
