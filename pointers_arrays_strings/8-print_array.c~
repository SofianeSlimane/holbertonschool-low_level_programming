#include "main.h"
#include <stdio.h>

/**
 * main - check the code for
 *
 * Return: Always 0.
 */

void print_array(int *a, int n)
{
  int i;
  for (i = 0; a[i] != '\0'; i++)
    {
      printf("%d", a[i]);
      _putchar(',');
      _putchar(' ');
    }
  _putchar('\n');
}
