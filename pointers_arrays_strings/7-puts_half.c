#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void puts_half(char *str)
{
  int i;
  int taille;
  

  for (taille = 0; str[taille] != '\0'; taille++)
    {
 
    }
  for (i = (taille - 1) / 2; i != '\0'; i++)
    {
      _putchar(str[i]);
    }
}
	
	 
	
