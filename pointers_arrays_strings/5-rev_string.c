#include "main.h"

void rev_string(char *s)
{
  int taille;
  int i;
  for (taille = 0; s[taille] != '\0'; taille++)
    {
    }

  if (taille > 0)
    {
  for (i = taille -1; s[i] != '\0'; i--)
    {
      
    }
    }
}
