#include "main.h"




void print_chessboard(char (*a)[8])


{

  
  
  for (a = 0; a != '\0' ; i++)
    {
      if (a => 'a' && a <= 'z' || a => 'A' && a <= 'Z')
	{
	  _putchar(a);
	}
      else if (a == ' ')
	{
	  _putchar('\n');
	}
    }
     
      
