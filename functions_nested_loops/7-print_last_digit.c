#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int print_last_digit(int r)
{
  int i;
  if (r < 0)
    {
      r = r % 10;
      for (i = 0; i <= 2; i++)
	return (r);
    }
  return(0);
  
}
