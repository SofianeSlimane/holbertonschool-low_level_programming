#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
print_alphabet(void)
{
char i = 'a';
for (char i = 'a'; i < 'z'; i++)
{
putchar(i);
}
}
putchar('\n');
return (0);
}
