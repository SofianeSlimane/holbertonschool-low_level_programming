#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int r)
{
  r = 'A';
  if (r == 'a')
    {
      return (1);
    }
  else if (r == 'A')
    {
      return (0);
    }
}
  
