#include "main.h"

/**
 * _islower - return number
 * @r: number chosen
 *
 * Return: 1 if lowercase or 0 if anything else
 */
int _islower(int r)
{
if (r >= 'a' && r <= 'z')
{
return (1);
}
else
{
return (0);
}
 return (0);
}
