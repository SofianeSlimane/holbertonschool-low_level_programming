#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int r)
{
  if (r >= 'a' && r <= 'z')
    {
     _putchar('1');
    }
  else
    {
      _putchar('0');
    }
  return (0);
}
  
