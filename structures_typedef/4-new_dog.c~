#include "dog.h"
#include <stdlib.h>

/**
 *
 *
 *
 */

dog_t *new_dog(char *name, float age, char *owner)
{
  char **p1 = &name;
  char **p2 = &owner;
  if (new_dog == NULL)
    {
      return (NULL);
    }
  

  new_dog.owner = p2;
  new_dog.name = p1;
  new_dog.age = age;
}
