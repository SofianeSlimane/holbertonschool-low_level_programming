#include "main.h"
#include <stdlib.h>


void *malloc_checked(unsigned int b)
{
  unsigned int *p;
  if (b = NULL)
    {
      exit(98);
    }
  p = malloc(b * sizeof(unsigned int));
  if (p == NULL)
    {
      exit(98); 
     }
  free(p);
  return (p);
}
  
  
