#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints arguments it receives
 * @argc: argc
 * @argv: argv
 * Return: 0
 */

int main(int argc, char *argc)
{
int i;
for (i = 0; i < argc; i++)
{
printf("%s\n", argv[i]);
}
return (0);
}
